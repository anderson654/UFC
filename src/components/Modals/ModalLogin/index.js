import React from "react";
import DefaultModal from "../DefaultModal";

export default function ModalLogin({ status }) {
    return (
        <DefaultModal status={true}>

        </DefaultModal>
    )
}